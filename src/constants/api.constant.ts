export const API_HOST = process.env.API_URL || "https://images-api.nasa.gov";

export const API = {
  search: "/search",
  asset: "/asset",
  metadata: "/metadata",
  album: "/album",
};

export default API;
